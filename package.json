{
  "type": "module",
  "scripts": {
    "dev": "node --inspect scripts/start.js dev",
    "start": "node scripts/start.js",
    "build": "node scripts/build.js",
    "format": "prettier --write \"{client,server,scripts,tests}/**/*.js\"",
    "lint": "prettier --check \"{client,server,scripts,tests}/**/*.js\"",
    "test": "uvu tests"
  },
  "dependencies": {
    "express": "^4.17.1",
    "jquery": "^3.6.0",
    "snowpack": "^3.0.13",
    "socket.io": "^3.1.2",
    "socket.io-client": "^3.1.2"
  },
  "devDependencies": {
    "@snowpack/plugin-sass": "^1.3.0",
    "prettier": "^2.2.1",
    "uvu": "^0.5.1"
  },
  "engines": {
    "node": "14.x",
    "npm": "7.x"
  }
}
